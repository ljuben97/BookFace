export class LikeModel {
  // tslint:disable-next-line:variable-name
  u_id: number;
  // tslint:disable-next-line:variable-name
  p_id: number;

  // tslint:disable-next-line:variable-name
  constructor(u_id, p_id) {
    this.p_id = p_id;
    this.u_id = u_id;
  }
}
